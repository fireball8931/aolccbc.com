version: '2'
services:
  web:
    build: .
#    ports:
#      - "8080:80"
    volumes:
      - ./www:/var/www/html
    restart: always

networks: 
  default: 
    external:
      name: nginx_proxy_manager_default 
